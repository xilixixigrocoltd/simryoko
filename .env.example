# ⚠️ REQUIRED: JWT Secret for authentication
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-min-32-chars

# Backend API URL (for B2B checkout/products/inventory)
BACKEND_API_URL=https://ciuh32wky.xigrocoltd.com/api

# Database (PostgreSQL recommended for production)
# DATABASE_URL=postgresql://user:password@localhost:5432/simryoko

# App
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_APP_NAME=SimRyoko
